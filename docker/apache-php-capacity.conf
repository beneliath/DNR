# Three PHP processes fit the 4 GiB envelope including a 1.25 GiB backup tmpfs.
<IfModule mpm_prefork_module>
    StartServers 2
    MinSpareServers 1
    MaxSpareServers 2
    ServerLimit 3
    MaxRequestWorkers 3
    MaxConnectionsPerChild 500
</IfModule>
